# sentraiq-core

A simple Python package for configurable **content moderation** (text + images).  
This is a **Proof of Concept (PoC)** version.

---

## 🚀 Installation

From **TestPyPI**:

```bash
pip install -i https://test.pypi.org/simple/ sentraiq-core